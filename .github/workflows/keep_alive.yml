name: Keep Replit Alive
on:
  schedule:
    - cron: '*/5 * * * *'  # Runs every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Replit
        run: |
          curl -s "https://YOUR_PROJECT.YOUR_USERNAME.repl.co" > /dev/null
